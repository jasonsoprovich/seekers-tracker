// Categorical dark-mode pair for the Main/Alt split — slots 1 (blue) and 2
// (orange) of the dataviz skill's validated default palette, adjacent in its
// documented order so the CVD/normal-vision gates hold (validated against
// this app's neutral-950/neutral-900 dark surfaces: `node
// scripts/validate_palette.js "#3987e5,#d95926" --mode dark --surface
// "#0a0a0a"` — all checks pass). Two series only, so no legend beyond a
// swatch pair is needed.
export const SERIES_MAIN = "#3987e5";
export const SERIES_ALT = "#d95926";
